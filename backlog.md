# Backlog

- Make the left panel resizable.
- Add a Stop button to cancel an in-progress streamed response.
- Add Retry for failed assistant requests.
- Export and import conversations as JSON or Markdown.
- Add a Copy response button to assistant messages.
- Add a light/dark theme toggle persisted locally.
- Add keyboard shortcuts, such as Cmd/Ctrl+Enter to send.
- Validate context-window and max-token values before sending.
- Warn when the newest message alone exceeds the context window.
- Add an option to collapse the settings panel on smaller screens.
- Improve accessibility with clearer focus states, status announcements, and `aria-busy` while streaming.
- Add request timing and token/character diagnostics.
- Add a configurable system prompt.
- Support non-streaming responses as a fallback.
- Add optional conversation persistence, disabled by default for privacy.
- Add a Copy button for the generated curl command.
- Make the “Generate curl command” section foldable.
